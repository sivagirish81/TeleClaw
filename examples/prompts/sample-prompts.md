# Sample TeleClaw Prompts

- "List available TeleClaw runbooks and summarize what each does."
- "Run `ssh.host_diagnose` for host `node-1.dev.example` and summarize issues."
- "Run `k8s.release_diagnose` with namespace `payments` and workload `checkout`."
- "Fetch status for job `job-abc123` and include concise findings."
